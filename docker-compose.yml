services:
  quiz:
    build: .
    stdin_open: true    # Allow interactive input
    tty: true           # Allocate terminal
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis

  redis:
    image: redis:alpine
    volumes:
      - redis_data:/data

volumes:
  redis_data:
